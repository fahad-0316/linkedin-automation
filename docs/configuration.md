# Configuration

Configuration reference for credentials, rate limits, storage, logging, and campaign defaults will be documented here.

Keep secrets in environment variables or a local secret manager. Never commit `.env` files.
