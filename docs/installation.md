# Installation

Installation instructions will be added with the first implementation release.

For local development, clone the repository, install the project dependencies, copy `.env.example` to `.env`, and run the documented checks.
