name: Tests

on:
  push:
  pull_request:

jobs:
  placeholder:
    name: Repository checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify scaffold
        run: test -f README.md && test -d src && test -d tests
